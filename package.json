{
  "version": "0.1.0",
  "scripts": {
    "init": "tree-sitter init",
    "prepare": "node scripts/generate-symbols.js",
    "gen": "tree-sitter generate",
    "test": "tree-sitter test",
    "build": "tree-sitter build"
  },
  "dependencies": {
    "tree-sitter-cli": "^0.25.10"
  }
}
